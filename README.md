# Hallucination-Detector
It builds a hybrid hallucination detection system that verifies claims using Wikipedia. It combines TF-IDF similarity, word overlap, and rule-based checks like entity matching and reversed relations to classify statements as VERIFIED, ABSTAINED, or HALLUCINATED.
